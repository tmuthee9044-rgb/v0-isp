import { getSql } from "./db"

export async function checkNetworkStatus(customerId: number) {
  const sql = await getSql()
}

export async function getNetworkStatistics() {
  const sql = await getSql()
}

export async function monitorBandwidth(customerId: number) {
  const sql = await getSql()
}
